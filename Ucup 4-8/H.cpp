#include<bits/stdc++.h>
using namespace std;
typedef pair<int,int> P;
int N = 125;
P a[N];
int main(){
	int T;
	cin >> T;
	int n;
	cin >> n;
	for (int i = 1; i <= n; i++){
		int x, y;
		cin >> x >> y;
		a[i] = {x, y};
	}
	for (int i = 1; i <= n; i++){
		int x = a[i].first, y = a[i].second;
		
	}
}
